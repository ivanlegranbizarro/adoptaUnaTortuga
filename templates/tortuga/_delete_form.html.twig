<form method="post" action="{{ path('app_tortuga_delete', {'id': tortuga.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tortuga.id) }}">
    <button class="btn">Delete</button>
</form>
